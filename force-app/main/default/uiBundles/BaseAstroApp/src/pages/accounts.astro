---
import AppLayout from '@layouts/AppLayout.astro';
import Card from '@components/Card.astro';
import RecordList from '@components/RecordList.tsx';
---
<AppLayout
  title="Accounts · Astro UI Bundle"
  eyebrow="Sales"
  heading="Accounts"
  subheading="Organizations in your org. Data is queried live via the UI API GraphQL endpoint."
>
  <Card title="All accounts" subtitle="Ordered by name">
    <RecordList
      client:load
      sobject="Account"
      columns={[
        { label: 'Name',     field: 'Name' },
        { label: 'Industry', field: 'Industry', muted: true },
        { label: 'Type',     field: 'Type',     muted: true },
        { label: 'Phone',    field: 'Phone',    muted: true, align: 'right' },
      ]}
    />
  </Card>
</AppLayout>
